from actor import Actor
from lib.controllers.hero_controller import HeroController

class Hero(Actor):
    def __init__(self, x, y, animation):
        super(x, y, animation, HeroController, pyhsicsController)
